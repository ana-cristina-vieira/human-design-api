# Copy this to .env and fill in your values

# n8n webhook URL (from your n8n workflow)
# Example: http://localhost:5678/webhook/human-design-lead
# n8n está no mesmo VPS → usa localhost
N8N_WEBHOOK_URL=http://localhost:5678/webhook/human-design-lead

# Port for the API (default 8000)
PORT=8000
